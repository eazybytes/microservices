# Copy to prod.env (gitignored) and fill in real values before running against prod.
# Prefer injecting these from a secrets manager / CI variable store rather than
# keeping a real prod.env file on disk long-term.
IMAGE_TAG=s9
SPRING_PROFILE=prod