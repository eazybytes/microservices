# Loaded automatically by `docker compose` when running from this directory.
# Safe to commit: local/dev-only values, never used for qa or prod.
IMAGE_TAG=s6
SPRING_PROFILE=default
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
